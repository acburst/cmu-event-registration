= form_for @organizer_list do |f|
  - if @organizer_list.errors.any?
    #error_explanation
      %h2= "#{pluralize(@organizer_list.errors.count, "error")} prohibited this organizer_list from being saved:"
      %ul
        - @organizer_list.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
